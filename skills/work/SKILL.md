---
name: work
description: Return from discussion or one-shot read-only mode to normal work.
---

# Work

Run `node ${CLAUDE_PLUGIN_ROOT}/scripts/control.mjs mode normal` from the project root and show the route message. This changes only Fabex routing; it does not widen native permissions or sandbox access.
