---
name: discussion
description: Enter persistent joint discussion on the canonical Codex SDK thread with a mechanical read-only sandbox.
---

# Discussion

Run Fabex `config`, then `control.mjs mode discussion --participants both` from the resolved workstream root and show the mode message. Apply the configured reply badge.

Remain read-only until `/work`. Submit every owner message once through the jointly skill's SDK protocol with the parity watchdog. The durable queue preserves order, and every turn resumes the canonical ID with the SDK's `read-only` sandbox. Converge only after exact `thread.started` verification and relay flags unedited.
