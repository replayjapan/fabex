---
name: discussion
description: Enter persistent read-only discussion mode until the user invokes work.
---

# Discussion

Run `node ${CLAUDE_PLUGIN_ROOT}/scripts/control.mjs mode discussion` from the project root and show the route message. Analyze without file changes, effectful commands, or other project effects until the user invokes `/work`.
