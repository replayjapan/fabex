---
name: discussionCodex
description: Enter persistent behaviorally read-only Codex relay mode using the canonical verified MCP thread.
---

# Discussion Codex

Run Fabex `status`, `config`, and `diagnose`, then `control.mjs mode discussion --participants codex` from the resolved workstream root and show the mode message. Apply the configured reply badge.

For every owner message, follow the jointly skill's canonical MCP protocol. Pass the owner's message verbatim, parity watchdog, and explicit no-effects instructions. Use only the begin-authorized tool and exact canonical threadId. Relay after PostToolUse verification, attribute Codex, relay flags unedited, and stay substantively silent. The thread remains workspace-write, so read-only is instructional. Mode changes never clear it.
