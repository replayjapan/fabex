---
name: discussionCodex
description: Enter persistent read-only Codex relay mode using the same continuous verified primary thread.
---

# Discussion Codex

Run Fabex `status`, `config`, and `diagnose`, then `control.mjs mode discussion --participants codex` from the resolved workstream root and show the mode message. Apply the configured reply badge.

For every owner message, follow the jointly skill's primary-thread begin, companion, and complete protocol. Pass the owner's message verbatim and the parity-watchdog invitation. Use the returned plan exactly: `--resume-last` only follows a matching pre-launch candidate, while an authorized checkpoint-seeded recovery may return `--fresh`. Never pass `--write`. Relay Codex's answer with attribution, relay visible continuity recovery plus scope or partnership-parity flags unedited, and stay substantively silent. Mode changes never clear the primary thread.
