---
name: askCodex
description: Ask Codex one behaviorally read-only question through the canonical verified MCP thread and relay it.
---

# Ask Codex

Run Fabex `status`, `config`, and `diagnose`, then `control.mjs mode ask-once --participants codex` and show the mode message. Apply the configured reply badge.

Follow the jointly skill's canonical MCP protocol. Pass the owner's question verbatim, parity watchdog, and explicit no-effects instructions. The canonical thread remains workspace-write, so read-only is instructional. Relay only after structured thread-ID verification, attribute Codex, relay flags unedited, and note that the next prompt restores the prior mode.
