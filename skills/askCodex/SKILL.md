---
name: askCodex
description: Ask Codex one read-only question through the canonical verified SDK thread and relay it.
---

# Ask Codex

Run Fabex `status`, `config`, and `diagnose`, then `control.mjs mode ask-once --participants codex` and show the mode message. Apply the configured reply badge.

Follow the jointly skill's SDK submit/status/result protocol. Pass the owner's question verbatim and parity watchdog. The controller resumes the canonical thread with `read-only` and verifies the exact `thread.started` ID. Attribute Codex, relay flags unedited, and note that the next prompt restores the prior mode.
